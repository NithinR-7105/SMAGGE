# ─── SMAGGE Requirements ─────────────────────────────────────────────────────
# Requires Python 3.12 (not 3.14 — CrewAI does not support 3.14 yet)
# Setup: py -3.12 -m venv venv && venv\Scripts\activate

# Core agent framework
crewai>=0.80.0,<2.0.0
crewai-tools>=0.4.0,<1.0.0

# LLM integrations
langchain>=0.2.0,<0.4.0
langchain-community>=0.2.0,<0.4.0
ollama>=0.1.8

# OCR (Analyst agent)
pytesseract>=0.3.10
Pillow>=10.0.0
opencv-python>=4.9.0.80

# Database
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0

# Lead data sources
requests>=2.31.0
httpx>=0.27.0

# Data handling
pandas>=2.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Utilities
rich>=13.0.0
loguru>=0.7.0

# ─── Phase 4: Dashboard ──────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0

# ─── Phase 3: API & Feedback Loop ────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0

# ─── Phase 2: Security Guard Layer ───────────────────────────────────────────
# nemoguardrails>=0.9.0  ← requires C++ build tools on Windows.
# Our custom security layer in security/ implements the same checks natively.
# The guardrails/config.yml + main.co files document the NeMo Guardrails
# integration pattern for portfolio/interview purposes.
